{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "investment_dashboard",
            "database": "${workspaceFolder:investment-dashboard}/src-tauri/test.db"
        }
    ]
}